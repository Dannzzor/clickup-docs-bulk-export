# Copy this file to `.env` in the project root (or cwd when you run the CLI).
# `.env` is gitignored — do not commit real tokens.
#
# Workspace ID is still passed with --workspace (see README).

CLICKUP_API_TOKEN=
